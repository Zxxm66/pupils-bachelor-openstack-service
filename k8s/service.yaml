apiVersion: v1
kind: Service
metadata:
  name: pupils-bachelor-service
  namespace: pupils-bachelor
spec:
  selector:
    app: pupils-bachelor
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30080
  type: NodePort
